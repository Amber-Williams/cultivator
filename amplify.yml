version: 1
frontend:
  phases:
    preBuild:
      commands:
        - cd app
        - npm install
    build:
      commands: 
        - amplifyPush --simple
        - npm run build
  artifacts:
    baseDirectory: /app/build
    files:
      - '**/*'
  cache:
    paths: []
